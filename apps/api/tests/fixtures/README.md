# API Test Fixtures

These fixtures are intentionally synthetic and deterministic.

Guidelines:

- use obviously synthetic names and contact data
- never copy production-like patient identifiers into tests
- keep ids and timestamps deterministic for stable assertions
- override only the fields relevant to the test case
