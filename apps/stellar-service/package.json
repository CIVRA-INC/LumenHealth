{
  "name": "@lumen/stellar-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "dev:server": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "node -e \"console.log('stellar lint: add stronger checks in milestone work')\""
  },
  "dependencies": {
    "@lumen/config": "^0.1.0",
    "@lumen/types": "^0.1.0",
    "@stellar/stellar-sdk": "^14.6.1",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^24.10.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.7"
  }
}
